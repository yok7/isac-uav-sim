# 第二周汇报布局：任务3/任务4

## 总体原则

延续第一周风格：

- 每页只讲一个点
- 标题尽量 5 字以内
- 一页一张主图
- 每页保留一句结论
- 不讲大段背景，不重复综述和 DOA 基础

本周核心主线：

```text
任务3：5G NR OFDM波形 + ISAC回波仿真
任务4：ULA/UPA阵列 + 低空UAV场景
```

建议总页数：9 页。

## 第1页：第二周进展

标题：

```text
第二周进展
```

副标题：

```text
通感一体化波形生成与多天线阵列建模
```

本周主线：

```text
NR波形 / ISAC仿真 / 阵列建模
```

## 第2页：目录

标题：

```text
目录
```

内容：

```text
01 NR波形
02 ISAC仿真
03 阵列建模
04 总结
```

## 第3页：NR波形

标题：

```text
NR波形
```

主图：

```text
results/sionna_learning/01_pusch_resource_grid.png
```

右侧短句：

```text
PUSCHConfig：配置5G NR上行资源网格
DMRS：导频资源，可用于信道估计/感知
MCS：决定调制编码方式
PUSCHTransmitter：生成OFDM基带波形
```

一句结论：

```text
已用 Sionna 生成符合 5G NR PUSCH 结构的 OFDM 资源网格和基带波形。
```

## 第4页：资源复用

标题：

```text
资源复用
```

建议画一个很简单的示意图：

```text
同一OFDM帧
├── 数据RE：通信传输
└── DMRS：信道估计 / 感知探测
```

可配图：

```text
results/sionna_learning/05_task3_dmrs_density_effect.png
```

一句结论：

```text
通感波形的关键不是另造信号，而是在同一帧内分配通信资源和感知资源。
```

## 第5页：参数影响

标题：

```text
参数影响
```

主图：

```text
results/sionna_learning/05_task3_rate_sensing_tradeoff.png
```

左下角写配置：

```text
SCS：15 / 30 / 60 kHz
RB：4 / 8 / 16
DMRS密度：0 / 1 / 2
帧结构：[0,14] / [0,10] / [2,12]
```

一句结论：

```text
带宽、导频密度、帧结构都会改变通信速率与感知能力的折中。
```

备注：

```text
这一页是理论设计扫描，不把它说成最终感知精度。
```

## 第6页：实际仿真

标题：

```text
实际仿真
```

主图：

```text
results/sionna_learning/07_task3_actual_rate_vs_range_rmse.png
```

右侧流程：

```text
Sionna生成OFDM资源网格
→ DMRS作为感知资源
→ 加入UAV单目标回波
→ 加噪声
→ 估计距离RMSE
```

关键结果：

```text
18组配置
每组80次Monte Carlo
GPU运行约20秒
最佳RMSE：0.232 m
```

一句结论：

```text
这一步从理论proxy推进到“发射波形—目标回波—距离估计误差”的实际仿真。
```

## 第7页：阵列场景

标题：

```text
阵列场景
```

主图：

```text
results/sionna_learning/03_array_uav_geometry.png
```

右侧参数：

```text
BS：[0, 0, 10] m
UAV：[120, 45, 80] m
距离：146.03 m
方位角：20.56°
俯仰角：28.64°
```

一句结论：

```text
已建立低空UAV目标与地面多天线基站的几何场景。
```

## 第8页：阵列响应

标题：

```text
阵列响应
```

主图二选一：

```text
results/sionna_learning/06_task4_upa_az_el_response.png
```

或：

```text
results/sionna_learning/03_ula_beam_pattern.png
```

公式：

```text
a(θ,φ)[m] = exp(j·2π/λ · r_mᵀu(θ,φ)) / √M
```

一句结论：

```text
不同目标方向对应不同阵列响应向量，这是后续DOA估计的基础。
```

## 第9页：总结

标题：

```text
总结
```

本周完成：

```text
1. 生成5G NR PUSCH OFDM基带波形
2. 区分通信数据资源和DMRS感知资源
3. 扫描SCS、DMRS密度和帧结构
4. 补充单目标ISAC距离RMSE仿真
5. 建立ULA/UPA阵列和低空UAV场景
```

一句话结论：

```text
本周把“会生成波形”推进到“能用同一OFDM帧做通信与单目标感知仿真”。
```

当前不足：

```text
目前只做了单目标距离估计，还没有加入Doppler、DOA闭环、多目标和杂波。
```

下周计划：

```text
接入阵列DOA估计
扩展range + angle联合估计
比较通信速率与感知误差
```

