# 第二周汇报内容建议

## 总体结构

建议 8 页，分 3 部分：

- NR波形
- 通感权衡
- 阵列建模

不要再重复第一周的综述和 DOA 基础，第二周重点体现“从会跑到会改参数”。

## 第1页：第二周进展

标题：

```text
第二周进展
低空无人机通感一体化波形与DOA估计
```

本周主线：

```text
NR波形 / 通感权衡 / 阵列建模
```

## 第2页：目录

```text
01 NR波形
02 通感权衡
03 阵列建模
04 总结
```

## 第3页：NR波形

标题：

```text
NR波形
```

左侧放图：

```text
results/sionna_learning/01_pusch_resource_grid.png
```

右侧文字：

```text
PUSCHConfig：配置5G NR上行资源
DMRS：导频，用于信道估计/感知探测
MCS：决定调制编码方式
PUSCHTransmitter：生成OFDM资源网格
```

底部一句话：

```text
本周重点：看懂5G NR波形从参数配置到资源网格生成的过程。
```

## 第4页：信道接口

标题：

```text
信道接口
```

左侧放图：

```text
results/sionna_learning/04_channel_response.png
```

右侧流程：

```text
输入：OFDM资源网格 x
信道：RayleighBlockFading
输出：接收信号 y，信道响应 h_freq
```

核心公式：

```text
Y[k,n] = H[k,n]X[k,n] + W[k,n]
```

一句话：

```text
目前已跑通OFDM波形生成和频域信道建模的最小闭环。
```

## 第5页：通感权衡

标题：

```text
通感权衡
```

左侧放图：

```text
results/sionna_learning/02_isac_waveform_tradeoff.png
```

右侧文字：

```text
改动参数：
SCS：15 / 30 / 60 kHz
RB数：4 / 8 / 16
DMRS密度：additional_position = 0 / 1 / 2
```

结论：

```text
带宽增大，距离分辨率变好；
导频增多，感知采样更密；
但通信资源会被导频占用。
```

## 第6页：阵列建模

标题：

```text
阵列建模
```

左侧放图：

```text
results/sionna_learning/03_array_uav_geometry.png
```

右侧文字：

```text
基站：ULA / UPA
目标：低空无人机
输出：阵元坐标 + 目标方向
```

场景参数：

```text
BS = [0, 0, 10] m
UAV = [120, 45, 80] m
range = 146.03 m
azimuth = 20.56°
elevation = 28.64°
```

## 第7页：阵列响应

标题：

```text
阵列响应
```

左侧放图：

```text
results/sionna_learning/03_ula_beam_pattern.png
```

右侧公式：

```text
a(θ,φ)[m] = exp(j·2π/λ · r_mᵀu(θ,φ)) / √M
```

一句话：

```text
DOA估计的核心，就是利用不同角度对应的阵列响应向量不同。
```

## 第8页：总结

标题：

```text
总结
```

本周完成：

```text
1. 生成5G NR PUSCH OFDM基带波形
2. 跑通OFDM频域信道接口
3. 扫描SCS/RB/DMRS参数，观察通感权衡
4. 建立低空无人机 + ULA/UPA阵列场景
```

本周一句话结论：

```text
通感一体化的第一步，是把通信资源网格、导频资源和阵列响应统一到同一个仿真链路里。
```

当前疑问：

```text
DMRS作为感知资源时，应该如何定义感知精度指标？
Sionna的信道响应如何进一步转成目标距离/角度估计？
多天线阵列结果如何与DOA算法闭环？
```

下周计划：

```text
接入DOA估计
构造UAV回波
比较通信/感知指标
```

